# 快看漫画沉浸式品牌落地页 · 设计规范

## 视觉命题

**「撕开分镜，进入下一格」**：以漫画分镜、印刷网点与撕纸切口为基础语汇，叠加赛博信号、街头标签和高饱和撞色。页面不是传统信息陈列，而是一条由“入场 → 世界观 → 角色 → 创作者生态 → 下载入口”组成的滚动叙事轨道。

## 配色系统

| 角色 | 色值 | 用法 |
|---|---|---|
| Void Black | `#07080B` | 页面主背景、沉浸氛围 |
| Carbon | `#111318` | 卡片与浮层 |
| Kuaikan Yellow | `#FFE600` | 主 CTA、重点数字、焦点态 |
| Neon Magenta | `#FF2EA6` | 角色属性、故障残影 |
| Acid Green | `#78FF69` | 数据与系统状态强调 |
| Signal White | `#F4F4F0` | 主标题与正文 |
| Muted Gray | `#92949C` | 次级信息 |

对比原则：正文使用白/灰对黑底；黄色只承担动作与关键结论，粉/绿作为短促撞色，避免三种高饱和色同时大面积出现。

## 字体建议

- 中文展示：`Noto Sans SC Black` / 思源黑体 Heavy，标题压缩行高至 0.88–0.95，形成海报感。
- 拉丁与数字：`Space Grotesk`，用于导航、编号、数据和系统标签。
- 章节编号：全大写、宽字距，搭配等宽回退 `ui-monospace`。
- 正文字号不低于 16px；交互标签不低于 14px。

## 栅格与形态

- 桌面端：12 栏，左右安全边距 32–56px；移动端：4 栏，边距 20px。
- 主容器最大宽度 1440px；全屏叙事段落保持 100svh。
- 卡片切角 22–34px，1px 半透明描边；核心卡片增加霓虹内发光。
- 纹理采用 halftone 网点、扫描线、噪点和细网格，透明度控制在 3%–12%。

## 动效节奏

- 首屏入场：0.9–1.4s，标题分层错位后归位；背景粒子持续低速漂移。
- 常规显隐：0.55–0.8s，使用 `power3.out`；层叙事切换 0.9–1.2s。
- 滚动叙事：章节容器固定 300vh，按滚动进度切换 3 个阶段。
- 角色墙：桌面端横向滚动；卡片悬浮 220–320ms，倾斜不超过 6°。
- 微交互：按钮磁吸幅度 6–10px，故障闪烁每次 80–140ms 且低频触发。
- `prefers-reduced-motion`：关闭视差、横向固定、故障闪烁与粒子旋转，保留直接内容切换。

## 交互与可访问性

- 所有 CTA 保持 44px 以上触控面积，并有清晰的键盘焦点环。
- 导航锚点支持平滑滚动；主内容可在脚本或 CDN 失效时正常阅读。
- 角色卡片支持键盘聚焦；不依赖悬浮才能读取关键信息。
- 图片具备描述性替代文本；颜色不作为唯一信息载体。

